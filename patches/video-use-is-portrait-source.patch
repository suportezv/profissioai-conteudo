diff --git a/helpers/render.py b/helpers/render.py
index 0d02cff..6a9ff01 100644
--- a/helpers/render.py
+++ b/helpers/render.py
@@ -140,7 +140,7 @@ def is_portrait_source(video: Path) -> bool:
              "-of", "csv=p=0", str(video)],
             capture_output=True, text=True, check=True,
         )
-        w, h = map(int, out.stdout.strip().split(","))
+        w, h = map(int, [f for f in out.stdout.strip().split(",") if f])
         return h > w
     except Exception:
         return False
